#include<stdio.h>
int main()
{
    int number;
    printf("Enter An Integer\n:");
    scanf("%d",&number);
    if(number%2==0)
{
    printf("%d is a Even Number",number);

}
else 
{
    printf("%d is a odd number",number);
}
}

